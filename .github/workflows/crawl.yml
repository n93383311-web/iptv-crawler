name: IPTV Crawler

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  crawl:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout MUST be first
    - uses: actions/checkout@v3

    # 2. Check for existing lock
    - name: Check for workflow lock
      run: |
        if [ -f ".workflow_lock" ]; then
          echo "Another workflow is running. Exiting..."
          exit 0
        fi

    # 3. Create lock
    - name: Create workflow lock
      run: echo "locked" > .workflow_lock

    # ---- your original steps ----

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4

    - name: Run Crawler
      run: python crawler.py

    - name: Test Links
      run: python tester.py

    - name: Build Playlist
      run: python build_playlist.py

    - name: Flatten Playlist
      run: python flatten_m3u.py

    # ---- end steps ----

    # 4. Remove lock ALWAYS
    - name: Remove workflow lock
      if: always()
      run: rm -f .workflow_lock

    # 5. Commit results
    - name: Commit and Push changes
      if: always()
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "Update crawler results" || echo "No changes"
        git pull --rebase
        git push
